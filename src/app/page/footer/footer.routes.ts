// Modules
import { Routes, RouterModule } from '@angular/router';

import { FooterComponent } from './footer.component';

export const FooterRoutes: Routes = [
    { path: 'page', component: FooterComponent }
];
